<div class="main_footer">
    <footer class="footer">
        <div class="footer-content">
            <div class="footer-section">
                  <a href="{{ routes.root_url }}" class="header__heading-link link link--text focus-inset">
            {%- if settings.logo != blank -%}
              <div class="header__heading-logo-wrapper">
                {%- assign logo_alt = settings.logo.alt | default: shop.name | escape -%}
                {%- assign logo_height = settings.logo_width | divided_by: settings.logo.aspect_ratio -%}
                {% capture sizes %}(max-width: {{ settings.logo_width | times: 2 }}px) 50vw, {{ settings.logo_width }}px{% endcapture %}
                {% capture widths %}{{ settings.logo_width }}, {{ settings.logo_width | times: 1.5 | round }}, {{ settings.logo_width | times: 2 }}{% endcapture %}
                {{ settings.logo | image_url: width: 600 | image_tag:
                  class: 'header__heading-logo motion-reduce',
                  widths: widths,
                  height: logo_height,
                  width: settings.logo_width,
                  alt: logo_alt,
                  sizes: sizes,
                  preload: true
                }}
              </div>
            {%- else -%}
              <span class="h2">{{ shop.name }}</span>
            {%- endif -%}
          </a>

            </div>

            <div class="footer-section">
                <h3>About Us</h3>
                <ul class="footer-links">
                  {% for link in linklists.main-menu.links %}
        <li>
          <a href="{{ link.url }}">{{ link.title }}</a>
          {% if link.links.size > 0 %}
            <ul class="dropdown">
              {% for child in link.links %}
                <li><a href="{{ child.url }}">{{ child.title }}</a></li>
              {% endfor %}
            </ul>
          {% endif %}
        </li>
      {% endfor %}
                </ul>
            </div>

<div class="footer-section">
  <div class="footer-column">
    <h3>Furniture Sets</h3>
    <ul class="footer-links">
      {% for link in linklists.main-menu.links %}
      <li>
        <a href="{{ link.url }}">{{ link.title }}</a>
        {% if link.links.size > 0 %}
        <ul class="dropdown">
          {% for child in link.links %}
          <li><a href="{{ child.url }}">{{ child.title }}</a></li>
          {% endfor %}
        </ul>
        {% endif %}
      </li>
      {% endfor %}
    </ul>
  </div>
  <div class="footer-column">
    <h3>Customer Service</h3>
    <ul class="footer-links">
      <li><a href="#">Our Servesides</a></li>
      <li><a href="#">FAQs</a></li>
      <li><a href="#">+971 528531750</a></li>
    </ul>
  </div>
</div>

            <div class="footer-section join-section">
                <h2>Join Bless Brothers</h2>
                <p>Get exclusive offers, inspiration, and lots more to help bring your ideas to life.</p>
                
                <form class="subscribe-form">
                    <input type="email" placeholder="Email Address" required>
                    <button type="submit">Submit</button>
                </form>

                <div class="address">
                    Goshi Warehouses City<br>
                    Warehouse 28 - 22nd St - Al Quoz<br>
                    Al Quoz Industrial Area 3 - Dubai
                </div>

                <div class="social-icons">
                    <a href="#" aria-label="TikTok">TT</a>
                    <a href="#" aria-label="Facebook">FB</a>
                    <a href="#" aria-label="Instagram">IG</a>
                </div>
            </div>
        </div>
    </footer>
</div>


    <div class="bottom-bar">
        <div>© Copyright 2024 Bless Brothers. All rights reserved.</div>
        <div class="bottom-bar-links">
            <a href="#">Terms of Use</a>
            <a href="#">Privacy Policy</a>
            <a href="#">Cookie Policy</a>
            <a href="#">Cookie Settings</a>
        </div>
    </div>
<style>
   * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: Arial, sans-serif;
        }
.main_footer{
   background: #f4eee1;
}
        .footer {
           
            padding: 40px 20px;
            max-width: 1200px;
            margin: 0 auto;
        }

        .footer-content {
            display: grid;
            grid-template-columns: repeat(4, 1fr);
            gap: 30px;
            margin-bottom: 40px;
        }

        .logo {
            color: #008080;
            font-size: 24px;
            font-weight: bold;
            text-decoration: none;
        }

        .footer-section h3 {
            color: #333;
            margin-bottom: 20px;
            font-size: 16px;
        }

        .footer-links {
            list-style: none;
        }

        .footer-links li {
            margin-bottom: 12px;
        }

        .footer-links a {
            color: #666;
            text-decoration: none;
            font-size: 14px;
        }

        .footer-links a:hover {
            color: #008080;
        }

        .join-section h2 {
            font-size: 18px;
            margin-bottom: 15px;
        }

        .join-section p {
            color: #666;
            font-size: 14px;
            margin-bottom: 20px;
        }

        .subscribe-form {
            display: flex;
            gap: 10px;
            margin-bottom: 20px;
        }

        .subscribe-form input {
            flex: 1;
            padding: 8px 12px;
            border: 1px solid #ddd;
        }

        .subscribe-form button {
            background: #008080;
            color: white;
            border: none;
            padding: 8px 20px;
            cursor: pointer;
        }

        .address {
            color: #666;
            font-size: 14px;
            margin-bottom: 20px;
        }

        .social-icons {
            display: flex;
            gap: 15px;
        }

        .social-icons a {
            width: 30px;
            height: 30px;
            background: #333;
            color: white;
            display: flex;
            align-items: center;
            justify-content: center;
            text-decoration: none;
            border-radius: 3px;
        }

        .bottom-bar {
            background: #000;
            color: white;
            padding: 15px 20px;
            font-size: 14px;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .bottom-bar-links {
            display: flex;
            gap: 20px;
        }

        .bottom-bar a {
            color: white;
            text-decoration: none;
        }

        @media (max-width: 768px) {
            .footer-content {
                grid-template-columns: 1fr 1fr;
            }
        }

        @media (max-width: 480px) {
            .footer-content {
                grid-template-columns: 1fr;
            }

            .subscribe-form {
                flex-direction: column;
            }

            .bottom-bar {
                flex-direction: column;
                gap: 10px;
                text-align: center;
            }

            .bottom-bar-links {
                flex-direction: column;
                gap: 10px;
            }
        }
  </style>
